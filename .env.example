# ============================================
# NestJS v11 Template - Environment Variables
# ============================================
# Copy this file to .env and update with your values
# cp .env.example .env

# ============================================
# Application Configuration
# ============================================

# Application Environment
# Options: development, production, test
# Default: development
NODE_ENV=development

# Server Port
# Default: 3000
PORT=3000

# ============================================
# Database Configuration (REQUIRED)
# ============================================

# MongoDB Connection URI
# Required: Yes
# Format: mongodb://[username:password@]host[:port][/database][?options]
# Examples:
#   - Local: mongodb://localhost:27017/nestjs_template
#   - Docker: mongodb://mongodb:27017/nestjs_template
#   - Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017/nestjs_template

# ============================================
# Throttling Configuration
# ============================================

# Throttle Time Window (in seconds)
# How long the throttle window lasts
# Default: 60
THROTTLE_TTL=60

# Throttle Limit
# Maximum number of requests allowed per time window
# Default: 10
THROTTLE_LIMIT=10

# ============================================
# Cache Configuration
# ============================================

# Cache Time To Live (in seconds)
# How long cached data should be stored
# Default: 300 (5 minutes)
CACHE_TTL=300

# Cache Maximum Items
# Maximum number of items to store in cache
# Default: 100
CACHE_MAX=100

# ============================================
# JWT Configuration (Optional)
# ============================================

# JWT Secret Key
# Used for signing JWT tokens
# Optional: Leave empty if not using JWT authentication
# Recommended: Use a strong random string in production
JWT_SECRET=your-secret-key-change-in-production

# JWT Expiration Time
# How long JWT tokens remain valid
# Format: number + unit (e.g., 1d, 2h, 30m, 60s)
# Default: 1d (1 day)
JWT_EXPIRES_IN=1d

# ============================================
# Additional Notes
# ============================================
# 
# 1. MONGODB_URI is REQUIRED - the application will not start without it
# 2. All other variables are optional and have default values
# 3. For production, always set:
#    - NODE_ENV=production
#    - A strong JWT_SECRET
#    - Appropriate throttle and cache limits
# 4. Never commit your .env file to version control
# 5. Use .env.local for local overrides (higher priority than .env)
